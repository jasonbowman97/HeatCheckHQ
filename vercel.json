{
  "framework": "nextjs",
  "installCommand": "rm -rf node_modules 2>/dev/null; pnpm install --no-frozen-lockfile",
  "buildCommand": "next build",
  "crons": [
    {
      "path": "/api/refresh",
      "schedule": "0 6,18 * * *"
    }
  ]
}
